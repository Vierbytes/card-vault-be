# CardVault Backend Environment Variables
# Copy this file to .env and fill in your values

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection
# I learned MongoDB Atlas gives you a free cluster - the connection string looks like this
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/cardvault?retryWrites=true&w=majority

# JWT Secret - used to sign authentication tokens
# Generate a random string for production (I used: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# TCGdex is used for card data and pricing - no API key needed!
# Docs: https://tcgdex.dev

# Frontend URL - for CORS configuration
CLIENT_URL=http://localhost:5173
